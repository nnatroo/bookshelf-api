const express = require('express');\nconst router = express.Router();\nconst { getAllBooks, getBookById, createBook, updateBook, deleteBook } = require('../controllers/bookController');\nconst { bookValidationRules } = require('../validators/bookValidator');\n\nrouter.get('/', getAllBooks);\nrouter.get('/:id', getBookById);\nrouter.post('/', bookValidationRules, createBook);\nrouter.put('/:id', bookValidationRules, updateBook);\nrouter.delete('/:id', deleteBook);\n\nmodule.exports = router;